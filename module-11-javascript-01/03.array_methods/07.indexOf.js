// The indexOf() method returns the first index (position) of a specified value.

let colors = ['red', 'blue', 'violet', 'green', 'yellow'];

let indexOfViolet = colors.indexOf("violet");

console.log(indexOfViolet);

// start searching at a specific position
let result = colors.indexOf("green", 3);
console.log(result);